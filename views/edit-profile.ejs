<!DOCTYPE html>
<html lang="en">
<head>
  <title>Edit Profile</title>
  <link rel="stylesheet" href="/styles.css">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
  <% if (post && post._id) { %>
    <header><h2>Edit Profile</h2></header>
    <main>
      <p>Editing as: <%= user.username %> (ID: <%= user._id %>)</p>
      <form method="POST" action="/profiles/<%= post._id %>?_method=PUT" autocomplete="off">
        <div>
          <label for="title">Title:</label>
          <input type="text" id="title" name="title" value="<%= post.title %>" placeholder="Title" required>
        </div>
        <div>
          <label for="platform">Platform:</label>
          <input type="text" id="platform" name="platform" value="<%= post.platform %>" placeholder="Platform" required>
        </div>
        <div>
          <label for="contentType">Content Type:</label>
          <input type="text" id="contentType" name="contentType" value="<%= post.contentType %>" placeholder="Content Type" required>
        </div>
        <div>
          <label for="content">Content:</label>
          <textarea id="content" name="content" placeholder="Content" required><%= post.content %></textarea>
        </div>
        <button type="submit">Update Profile</button>
      </form>
    </main>
  <% } else { %>
    <header><h2>Edit Profile</h2></header>
    <main><p>Profile data is unavailable.</p></main>
  <% } %>
</body>
</html>