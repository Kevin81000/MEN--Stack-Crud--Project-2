<!DOCTYPE html>
<html lang="en">
<head>
  <title>Edit Profile</title>
  <link rel="stylesheet" href="/styles.css">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
  <% if (post && post._id) { %>
    <section>
      <h2>Edit Profile</h2>
      <p>Editing as: <%= user.username %> (ID: <%= user._id %>)</p>
      <form method="POST" action="/profiles/<%= post._id %>?_method=PUT" autocomplete="off">
        <input type="text" name="title" placeholder="Title" required>
        <input type="text" name="platform"  placeholder="Platform" required>
        <input type="text" name="contentType" placeholder="Content Type" required>
        <textarea name= "Content" placeholder= "Content"></textarea>
        <button type="submit">Update Profile</button>
      </form>
    </section>
  <% } else { %>
    <section>
      <h2>Edit Profile</h2>
      <p>Profile data is unavailable.</p>
    </section>
  <% } %>
</body>
</html>