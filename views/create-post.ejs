<!DOCTYPE html>
<html lang="en">
<head>
  <title>Add New Profile</title>
  <link rel="stylesheet" href="/styles.css">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
  <% if (!user) { %>
    <p>Please log in to create a profile. <a href="/auth/login">Login</a></p>
  <% } else { %>
    <h2>Add New Profile</h2>
    <form method="POST" action="/profiles/addnew">
      <input type="text" name="title" placeholder="Title" required>
      <input type="text" name="platform" placeholder="Platform" required>
      <input type="text" name="contentType" placeholder="Content Type" required>
      <textarea name="content" placeholder="Content"></textarea>
      <button type="submit">Create Profile</button>
    </form>
    <% if (typeof error !== 'undefined' && error) { %>
      <p style="color: red;"><%= error %></p>
    <% } %>
  <% } %>
</body>
</html>